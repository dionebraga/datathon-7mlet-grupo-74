# ---- Python ----
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
.installed.cfg
*.egg

# ---- Virtual environments ----
.venv/
venv/
env/
ENV/
.python-version

# ---- Testing / coverage ----
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
.mypy_cache/
.ruff_cache/

# ---- Jupyter ----
.ipynb_checkpoints/

# ---- MLflow / experiment tracking ----
mlruns/
mlartifacts/
.mlflow/

# ---- Secrets / environment ----
.env
.env.*
!.env.example
*.key
*.pem
secrets/

# ---- Kaggle credentials ----
kaggle.json
.kaggle/

# ---- Data (large / generated artifacts are NOT versioned) ----
# We version small documented descriptors (README, schema, golden set) but never
# raw downloads, generated tables, provenance or big binaries.
data/kaggle/raw/
data/**/*.parquet
data/**/*.zip
data/**/*.csv
data/**/*.json
!data/**/schema*.json
!data/golden_set/*.jsonl
!data/**/*.md
# Generated artifacts of the run
artifacts/
*.joblib
*.pkl
*.pt
*.pth
*.onnx

# ---- Pitch deck build scratch (node_modules, render PNGs, scripts) ----
.pptx-build/

# ---- Material de apoio da gravação (orientação pessoal, não é entregável) ----
# Roteiro e runbook do vídeo: notas de condução para quem grava, não documentação
# do projeto. Ficam no disco, fora do repositório.
docs/roteiro-gravacao.md
docs/runbook-gravacao.md

# ---- OS / editor ----
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp

# ---- caracol bake debug artifacts (regenerated; never versioned) ----
.hero-baked.jpg
_mask*.jpg
_bake.png
frontend/public/_bake.png
*.pptx.bak-*

# Office lock/temp files
~$*
